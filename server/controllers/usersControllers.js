const models = require('../models/usersModels');

const usersController = {};

usersController.getUserTemplates = (req, res, next) => {
  // write code here

  next();
};

usersController.getUser = (req, res, next) => {
  // write code here

  next();
};



module.exports = usersController;
